From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: patch <patch@example.com>
Date: Fri, 8 May 2026 15:58:58 +0800
Subject: [PATCH] AFK network optimization


diff --git a/net/minecraft/commands/Commands.java b/net/minecraft/commands/Commands.java
index 6d07881b8076139d02eccb7e16b870e6cc003bf2..c209c03be447f607ada2bbf719a0e1c1d9776d0d 100644
--- a/net/minecraft/commands/Commands.java
+++ b/net/minecraft/commands/Commands.java
@@ -235,6 +235,7 @@ public class Commands {
         //ReloadCommand.register(this.dispatcher); // Folia - region threading
         RecipeCommand.register(this.dispatcher);
         FetchProfileCommand.register(this.dispatcher);
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkStatsCommand.register(this.dispatcher); // DeerFolia - AFK network optimization stats
         ReturnCommand.register(this.dispatcher); // Folia - region threading - TODO later
         RideCommand.register(this.dispatcher); // Folia - region threading - TODO later
         RotateCommand.register(this.dispatcher); // Folia - region threading - TODO later
diff --git a/net/minecraft/network/Connection.java b/net/minecraft/network/Connection.java
index d43be966a04456ba1a85c20f8b12f91de62a873d..9be8332208ca525c0139beaaa2b7f28b0ecbbb5c 100644
--- a/net/minecraft/network/Connection.java
+++ b/net/minecraft/network/Connection.java
@@ -75,6 +75,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
     // Spigot end
     private volatile @Nullable PacketListener disconnectListener;
     private volatile @Nullable PacketListener packetListener;
+    private volatile ProtocolInfo<?> deerFoliaOutboundProtocol = INITIAL_PROTOCOL; // DeerFolia - track outbound protocol for packet size estimation
     private @Nullable DisconnectionDetails disconnectionDetails;
     private boolean encrypted;
     private final java.util.concurrent.atomic.AtomicBoolean disconnectionHandled = new java.util.concurrent.atomic.AtomicBoolean(false); // Folia - region threading - may be called concurrently during configuration stage
@@ -86,6 +87,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
     private boolean handlingFault;
     private volatile @Nullable DisconnectionDetails delayedDisconnect;
     private @Nullable BandwidthDebugMonitor bandwidthDebugMonitor;
+    private volatile int deerFoliaCompressionThreshold = -1; // DeerFolia - track compression threshold for packet size estimation
     public String hostname = ""; // CraftBukkit - add field
     public boolean handleConnectionDisconnectOnNextTick = false; // Paper - Force kill connection ticking
     // Paper start - NetworkClient implementation
@@ -102,6 +104,14 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
         }
         return null;
     }
+
+    public final ProtocolInfo<?> getDeerFoliaOutboundProtocol() {
+        return this.deerFoliaOutboundProtocol;
+    }
+
+    public final int getDeerFoliaCompressionThreshold() {
+        return this.deerFoliaCompressionThreshold;
+    }
     // Paper end - add utility methods
     // Paper start - packet limiter
     protected final Object PACKET_LIMIT_LOCK = new Object();
@@ -372,6 +382,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
         if (protocol.flow() != this.getSending()) {
             throw new IllegalStateException("Invalid outbound protocol: " + protocol.id());
         } else {
+            this.deerFoliaOutboundProtocol = protocol; // DeerFolia - track outbound protocol for packet size estimation
             UnconfiguredPipelineHandler.OutboundConfigurationTask configMessage = UnconfiguredPipelineHandler.setupOutboundProtocol(protocol);
             BundlerInfo bundlerInfo = protocol.bundlerInfo();
             if (bundlerInfo != null) {
@@ -440,6 +451,14 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
             return;
         }
 
+        // DeerFolia start - suppress non-essential AFK packets
+        final net.minecraft.server.level.ServerPlayer deerFoliaPlayer = this.getPlayer();
+        if (deerFoliaPlayer != null && cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.shouldSuppress(deerFoliaPlayer, packet, this)) {
+            packet.onPacketDispatchFinish(deerFoliaPlayer, null);
+            return;
+        }
+        // DeerFolia end - suppress non-essential AFK packets
+
         packet.onPacketDispatch(this.getPlayer());
         if (false && connected && (InnerUtil.canSendImmediate(this, packet) // Folia - region threading - connection fixes
             || (io.papermc.paper.util.MCUtil.isMainThread() && packet.isReady() && this.pendingActions.isEmpty()
@@ -866,6 +885,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
 
     public void setupCompression(final int threshold, final boolean validateDecompressed) {
         if (threshold >= 0) {
+            this.deerFoliaCompressionThreshold = threshold; // DeerFolia - track compression threshold for packet size estimation
             com.velocitypowered.natives.compression.VelocityCompressor compressor = com.velocitypowered.natives.util.Natives.compress.get().create(io.papermc.paper.configuration.GlobalConfiguration.get().misc.compressionLevel.or(-1)); // Paper - Use Velocity cipher
             if (this.channel.pipeline().get(HandlerNames.DECOMPRESS) instanceof CompressionDecoder compressionDecoder) {
                 compressionDecoder.setThreshold(compressor, threshold, validateDecompressed); // Paper - Use Velocity cipher
@@ -880,6 +900,7 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
             }
             this.channel.pipeline().fireUserEventTriggered(io.papermc.paper.network.ConnectionEvent.COMPRESSION_THRESHOLD_SET); // Paper - Add Channel initialization listeners
         } else {
+            this.deerFoliaCompressionThreshold = -1; // DeerFolia - track compression threshold for packet size estimation
             if (this.channel.pipeline().get(HandlerNames.DECOMPRESS) instanceof CompressionDecoder) {
                 this.channel.pipeline().remove(HandlerNames.DECOMPRESS);
             }
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 5d4310dab55cc960ccfce279e656e7b740f3d5c0..7e45473ee52b9e0bff2e3f964b7533965faab9ca 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -3037,6 +3037,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
     public void resetLastActionTime() {
         this.lastActionTime = Util.getMillis();
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleBaseActivity(this); // DeerFolia - AFK network optimization
     }
 
     public ServerStatsCounter getStats() {
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 4924915ec3adf4d68ca537518fdffa52ec926bf7..1dee97257d0a64d0ef0f465a5be7b53380dfef87 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -568,6 +568,14 @@ public class ServerGamePacketListenerImpl
         if (containsInvalidValues(packet.position().x(), packet.position().y(), packet.position().z(), packet.yRot(), packet.xRot())) {
             this.disconnect(Component.translatable("multiplayer.disconnect.invalid_vehicle_movement"), org.bukkit.event.player.PlayerKickEvent.Cause.INVALID_VEHICLE_MOVEMENT); // Paper - kick event cause
         } else if (!this.updateAwaitingTeleport() && this.hasClientLoaded()) {
+            // DeerFolia start - AFK look and movement activity
+            Entity deerFoliaVehicle = this.player.getRootVehicle();
+            final boolean deerFoliaMoved = deerFoliaVehicle != this.player
+                && (packet.position().x() != deerFoliaVehicle.getX() || packet.position().y() != deerFoliaVehicle.getY() || packet.position().z() != deerFoliaVehicle.getZ());
+            final boolean deerFoliaLooked = deerFoliaVehicle != this.player
+                && (packet.yRot() != deerFoliaVehicle.getYRot() || packet.xRot() != deerFoliaVehicle.getXRot());
+            cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleLookOrMoveActivity(this.player, deerFoliaMoved, deerFoliaLooked);
+            // DeerFolia end - AFK look and movement activity
             Entity vehicle = this.player.getRootVehicle();
             // Paper start - Don't allow vehicle movement from players while teleporting
             if (this.awaitingPositionFromClient != null || this.player.isImmobile() || vehicle.isRemoved()) {
@@ -1500,6 +1508,16 @@ public class ServerGamePacketListenerImpl
         if (containsInvalidValues(packet.getX(0.0), packet.getY(0.0), packet.getZ(0.0), packet.getYRot(0.0F), packet.getXRot(0.0F))) {
             this.disconnect(Component.translatable("multiplayer.disconnect.invalid_player_movement"), org.bukkit.event.player.PlayerKickEvent.Cause.INVALID_PLAYER_MOVEMENT); // Paper - kick event cause
         } else {
+            // DeerFolia start - AFK look and movement activity
+            final boolean deerFoliaMoved = packet.hasPosition()
+                && (packet.getX(this.player.getX()) != this.player.getX()
+                || packet.getY(this.player.getY()) != this.player.getY()
+                || packet.getZ(this.player.getZ()) != this.player.getZ());
+            final boolean deerFoliaLooked = packet.hasRotation()
+                && (packet.getYRot(this.player.getYRot()) != this.player.getYRot()
+                || packet.getXRot(this.player.getXRot()) != this.player.getXRot());
+            cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleLookOrMoveActivity(this.player, deerFoliaMoved, deerFoliaLooked);
+            // DeerFolia end - AFK look and movement activity
             ServerLevel level = this.player.level();
             if (!this.player.wonGame && !this.player.isImmobile()) { // CraftBukkit
                 if (this.tickCount == 0) {
@@ -2328,6 +2346,7 @@ public class ServerGamePacketListenerImpl
 
     @Override
     public void onDisconnect(final DisconnectionDetails details) {
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.onDisconnect(this.player); // DeerFolia - AFK network optimization
         LOGGER.info("{} lost connection: {}", this.player.getPlainTextName(), details.reason().getString());
         // Paper start - Fix kick event leave message not being sent
         final net.kyori.adventure.text.Component quitMessage = details.quitMessage().map(io.papermc.paper.adventure.PaperAdventure::asAdventure).orElse(null);
@@ -2410,6 +2429,7 @@ public class ServerGamePacketListenerImpl
             return;
         }
         // CraftBukkit end
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleBaseActivity(this.player); // DeerFolia - AFK network optimization
         Optional<LastSeenMessages> unpackedLastSeen = this.unpackAndApplyLastSeen(packet.lastSeenMessages());
         if (!unpackedLastSeen.isEmpty()) {
             this.tryHandleChat(packet.message(), false, () -> {
@@ -2434,6 +2454,7 @@ public class ServerGamePacketListenerImpl
 
     @Override
     public void handleChatCommand(final ServerboundChatCommandPacket packet) {
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleBaseActivity(this.player); // DeerFolia - AFK network optimization
         this.tryHandleChat(packet.command(), true, () -> {
             // CraftBukkit start - SPIGOT-7346: Prevent disconnected players from executing commands
             if (this.player.hasDisconnected()) {
@@ -2473,6 +2494,7 @@ public class ServerGamePacketListenerImpl
 
     @Override
     public void handleSignedChatCommand(final ServerboundChatCommandSignedPacket packet) {
+        cn.lunadeer.mc.deerfolia.afknetwork.AfkNetworkManager.handleBaseActivity(this.player); // DeerFolia - AFK network optimization
         Optional<LastSeenMessages> unpackedLastSeen = this.unpackAndApplyLastSeen(packet.lastSeenMessages());
         if (!unpackedLastSeen.isEmpty()) {
             this.tryHandleChat(packet.command(), true, () -> {
